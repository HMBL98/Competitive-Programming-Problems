#include <iostream>
using namespace std;

#define optimizar_io ios_base::sync_with_stdio(0);cin.tie(0);

struct info{
	string first,second;
};
int n,caso;
info names[25];
int usado = 0;
int main(){
	optimizar_io;
	caso = 0;
	while(cin >> n && n){
		int cuenta = 0;
		for(int i = 0; i < n; i++){
			cin >> names[i].first >> names[i].second;
		}
		for(int i = 0; i < n; i++){

		}
	}
}
